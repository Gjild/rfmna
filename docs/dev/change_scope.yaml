{
  "schema_version": 1,
  "declared_frozen_ids": "none",
  "evidence": {
    "semver_bump": null,
    "decision_records": [],
    "conformance_updates": [],
    "migration_notes": [],
    "reproducibility_impact_statement_path": null
  },
  "notes": "P2-05 hardens `rfmna check` with additive deterministic JSON output (`--format json`), canonical schema `docs/spec/schemas/check_output_v1.json`, and typed loader/internal diagnostics. Default text grammar and run/check exit mapping remain contract-equivalent (`check`: 0 for no-error/warnings-only, 2 for any error path); no frozen artifact #9 semantic change is introduced."
}
